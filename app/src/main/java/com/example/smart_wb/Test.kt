package com.example.smart_wb

import android.icu.util.Calendar

class Test {
    val date = Calendar.getInstance()
    fun TitleFormatter() {
        var format: CharSequence
        CalendarDay
        day
        run {
            val calendar_view_format = SimpleDateFormat("yyyy MM")
            return calendar_view_format.format(date.time)
        }
    }
}